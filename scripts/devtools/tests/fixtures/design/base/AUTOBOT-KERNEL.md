# Sample — kernel

Authority for: the sample lifecycles.
Depends on: `AUTOBOT-THESIS.md`.

## 10. Lifecycles — the only place they are printed

```text
Sample        DRAFT → REVIEW → DONE | DROPPED
              REVIEW → DRAFT      (changes requested)
Gate          PENDING → PASSED | FAILED
```

## 11. Scope of guarantee

A `Gate` is PASSED only with evidence; see KERNEL §10 and F-12.

Background: `AUTOBOT-KERNEL.background.md`.
