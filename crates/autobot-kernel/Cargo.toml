[package]
name = "autobot-kernel"
description = "AutoBot kernel: aggregates, commit lanes, receipts, dispatch registers and lifecycles, without Kubernetes, async or I/O."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
schemars.workspace = true
serde.workspace = true
sha2.workspace = true
toml.workspace = true

[dev-dependencies]
autobot-devtools = { path = "../../scripts/devtools", default-features = false }
toml = { workspace = true, features = ["display"] }

[lints]
workspace = true
